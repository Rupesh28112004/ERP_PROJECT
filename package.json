{
  "name": "erp-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd project && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2"
  }
}